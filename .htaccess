RewriteEngine On

# 确保访问根目录时默认加载 index.html
DirectoryIndex index.html

# 如果请求的文件或目录不存在，才会应用下面的重写规则
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# 反代 GitHub 文件的规则
RewriteRule ^([^/]+)/([^/]+)/([^/]+)/(.*)$ index.php [L]
